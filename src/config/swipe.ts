/** Swipe gesture thresholds for TaskSwipeRow */
export const SWIPE_MAX_RIGHT = 80;       // px — reveal complete button (right swipe)
export const SWIPE_COMPLETE_THRESHOLD = 60;  // px — snap to complete reveal
export const SWIPE_REVEAL_THRESHOLD = -160;  // px — reveal edit + delete (left swipe)
export const SWIPE_DELETE_THRESHOLD = -250;  // px — auto-delete on extreme left swipe
