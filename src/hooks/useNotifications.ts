import { useEffect } from 'react';

// TODO (Phase 10): Implement notification scheduling
// - Request permissions, schedule daily reminders, goal milestone alerts
export function useNotifications() {
  useEffect(() => {
    // stub — implemented in Phase 10
  }, []);
}
